#pragma once
class Obstruction
{
public: void random_obs();

private:
	   sf::Texture obstruct;
	   if (!oceanTexture.loadFromFile("img/.....")) {
		   printf("loading failure");
	   }
};
void Obstruction::random_obs() {
		
}

